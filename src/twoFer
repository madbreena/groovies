import org.apache.tools.ant.types.selectors.SelectSelector

class TwoFer {

    static String twoFer(String name) {
        if (name) return "One for $name, one for me.".toString()
        else return "One for you,one for me."
    }
